/** In typescript, interface is a syntactical contract
 *  It often helps in providing a standard structure that
 *  the deriving classes would follow.
 *  An user should have an email and userId.
 */
export interface User {
  email: string;
  userId: String;
}
